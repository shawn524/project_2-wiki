<div class="jumbotron">
  <h2>Definitely not wikipedia</h2>
</div>

<div class="container">
  <% @articles.each do |article| %>
    <div class="row">
      <a href="/article/<%= article["page_id"] %>" class="col-md-4 list-group-item"><%= article["page_title"] %></a>
    </div>
  <% end %>
</div>
